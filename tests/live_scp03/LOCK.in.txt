# Set lifecycle state of an applet AID to LOCKED (0x80). The simulator does
# not recognise an arbitrary applet AID, so this is expected to surface an
# explicit error SW (e.g. 6A82 / 6A88) -- the test validates that the shell
# routes the SET-STATUS APDU and reports the SW cleanly without aborting.
SCP03-SD
LOCK A0000005591010FFFFFFFF8900000100
EXIT
